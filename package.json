{
    "name": "gitgrade-ai-root",
    "version": "1.0.0",
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "backend": "cd backend && venv\\Scripts\\activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
        "install:all": "npm install --prefix frontend && cd backend && python -m venv venv && venv\\Scripts\\activate && pip install -r requirements.txt"
    }
}